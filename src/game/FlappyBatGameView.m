classdef FlappyBatGameView < handle

    properties (Access = public)
        MainWindow % (1,1)
    end

    properties (Access = private)
    end

    methods (Access = public)
        % Constructor
        function this = FlappyBatGameView()
        end

        function render(this, gameModel)
        end
    end
end